<Window
    x:Class="serial_console_dotnet.Controls.TextEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Text File Editor">

    <Grid Background="#151E2E" RowDefinitions="Auto, Auto, Auto, *, Auto">
        <!-- Custom TitleBar -->
        <Grid Grid.Row="0" x:Name="AppTitleBar" Background="#0C1324" Height="32" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" MinWidth="138" />
            </Grid.ColumnDefinitions>

            <!-- Icon & Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0" Spacing="8">
                <FontIcon Glyph="&#xE104;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource CyanAccentBrush}" FontSize="12" />
                <TextBlock x:Name="WindowTitleTextBlock" Text="Text Editor" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
            </StackPanel>

            <!-- Draggable Area Spacer -->
            <Grid Grid.Column="1" Background="Transparent" />

            <!-- System Caption controls spacer -->
            <Grid Grid.Column="2" MinWidth="138" />
        </Grid>

        <!-- Toolbar (Row 1) -->
        <Grid Grid.Row="1" Background="#0C1324" Height="40" Padding="8,4" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <Button x:Name="SaveBtn" Click="SaveBtn_Click" Style="{StaticResource OutlineButtonStyle}" Padding="10,6" ToolTipService.ToolTip="Save (Overwrite)">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74E;" FontFamily="Segoe Fluent Icons" FontSize="12" />
                        <TextBlock Text="Save" FontSize="11" />
                    </StackPanel>
                </Button>
                <Button x:Name="SaveAsBtn" Click="SaveAsBtn_Click" Style="{StaticResource OutlineButtonStyle}" Padding="10,6" ToolTipService.ToolTip="Save As Local File">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE792;" FontFamily="Segoe Fluent Icons" FontSize="12" />
                        <TextBlock Text="Save As..." FontSize="11" />
                    </StackPanel>
                </Button>
                <Button x:Name="FindBtn" Click="FindBtn_Click" Style="{StaticResource OutlineButtonStyle}" Padding="10,6" ToolTipService.ToolTip="Find Text">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE11A;" FontFamily="Segoe Fluent Icons" FontSize="12" />
                        <TextBlock Text="Find" FontSize="11" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Find Bar (Row 2 - Hidden by default) -->
        <Grid Grid.Row="2" x:Name="FindPanel" Background="#1D2939" Visibility="Collapsed" Padding="12,6" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBox x:Name="FindTextBox" Width="200" Height="28" PlaceholderText="Find text..." Padding="6,2" FontSize="11" KeyDown="FindTextBox_KeyDown" />
                <Button Click="FindNext_Click" Style="{StaticResource OutlineButtonStyle}" Height="28" Padding="10,0">
                    <FontIcon Glyph="&#xE1FD;" FontFamily="Segoe Fluent Icons" FontSize="10" />
                </Button>
                <Button Click="FindPrev_Click" Style="{StaticResource OutlineButtonStyle}" Height="28" Padding="10,0">
                    <FontIcon Glyph="&#xE1FC;" FontFamily="Segoe Fluent Icons" FontSize="10" />
                </Button>
                <Button Click="CloseFind_Click" Style="{StaticResource OutlineButtonStyle}" Height="28" Padding="10,0" BorderThickness="0">
                    <FontIcon Glyph="&#xE10A;" FontFamily="Segoe Fluent Icons" FontSize="10" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Editor Area (Row 3) -->
        <Grid Grid.Row="3" Background="#0C1324" Margin="12" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Line Numbers Column -->
            <ScrollViewer x:Name="LineNumbersScrollViewer" Grid.Column="0" 
                          VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled"
                          Background="#080D1A" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,1,0"
                          Padding="8,10,6,10" Width="44">
                <TextBlock x:Name="LineNumbersTextBlock" Text="1" FontFamily="Consolas" FontSize="12" Foreground="{StaticResource TextMutedBrush}" TextAlignment="Right" />
            </ScrollViewer>

            <!-- Text Area -->
            <TextBox x:Name="EditTextBox" Grid.Column="1" AcceptsReturn="True" TextWrapping="NoWrap"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="12" Foreground="#F2F4F7"
                     Background="Transparent" BorderThickness="0" Padding="8,10"
                     SelectionChanged="EditTextBox_SelectionChanged" TextChanged="EditTextBox_TextChanged" Loaded="EditTextBox_Loaded" />
        </Grid>

        <!-- Bottom Status Bar (Row 4) -->
        <Grid Grid.Row="4" Background="#0C1324" Height="26" Padding="12,2" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left metrics info -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <TextBlock x:Name="CursorPositionText" Text="Ln 1, Col 1" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
                <TextBlock x:Name="CharCountText" Text="0 characters" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
            </StackPanel>

            <!-- Right layout metrics -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <TextBlock x:Name="FileTypeText" Text="Plain text" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
                <TextBlock Text="100%" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
                <TextBlock Text="Unix (LF)" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
                <TextBlock Text="UTF-8" FontSize="10" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
