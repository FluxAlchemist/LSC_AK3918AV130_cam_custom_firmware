<UserControl
    x:Class="serial_console_dotnet.Controls.UploadDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:serial_console_dotnet.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives"
    mc:Ignorable="d">

    <!-- Resources moved to App.xaml -->

    <Grid x:Name="UploadOverlay" Background="#A0000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border Width="500" VerticalAlignment="Center" Background="#151E2E" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" HorizontalAlignment="Center" Padding="20">
            <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto">
                
                <!-- Dialog Header -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE898;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource CyanAccentBrush}" FontSize="18" />
                        <TextBlock Text="Upload to Device (UART)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                    </StackPanel>
                    <Button Grid.Column="1" Click="CloseUploadDialog_Click" Style="{StaticResource OutlineButtonStyle}" Padding="6" BorderThickness="0">
                        <FontIcon Glyph="&#xE10A;" FontFamily="Segoe Fluent Icons" FontSize="14" />
                    </Button>
                </Grid>

                <!-- Remote Target Path Input -->
                <StackPanel Grid.Row="1" Spacing="4" Margin="0,0,0,12">
                    <TextBlock Text="Target Path on Device (File or Folder)" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBox x:Name="UploadTargetPathTextBox" Text="/tmp/uploaded_file" FontFamily="Consolas" Height="36" VerticalAlignment="Center" />
                </StackPanel>

                <!-- FTP Switch Panel -->
                <StackPanel x:Name="FtpSwitchPanel" Grid.Row="2" Visibility="Collapsed" Margin="0,0,0,12">
                    <ToggleSwitch x:Name="UseFtpToggle" Header="Use FTP Transfer" IsOn="False" Toggled="UseFtpToggle_Toggled" FontSize="12" />
                    
                    <StackPanel x:Name="FtpCredentialsPanel" Visibility="Collapsed" Spacing="8" Margin="0,8,0,0">
                        <Grid ColumnDefinitions="*, *">
                            <StackPanel Spacing="4" Margin="0,0,4,0">
                                <TextBlock Text="FTP Username" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                <TextBox x:Name="FtpUsernameTextBox" Text="root" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="4" Margin="4,0,0,0">
                                <TextBlock Text="FTP Password" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                <PasswordBox x:Name="FtpPasswordBox" Password="" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </StackPanel>

                <!-- MD5 Verification Switch -->
                <StackPanel x:Name="VerifyMd5Panel" Grid.Row="3" Margin="0,0,0,12">
                    <ToggleSwitch x:Name="VerifyMd5Toggle" Header="Verify with md5sum after upload" IsOn="True" FontSize="12" />
                </StackPanel>

                <!-- File Dropzone -->
                <Border Grid.Row="4" x:Name="UploadDropZoneBorder" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" Background="#0C1324" CornerRadius="6" Padding="16" DragOver="DropZone_DragOver" Drop="DropZone_Drop" PointerPressed="DropZone_Click" Height="100" Margin="0,0,0,12">
                    <primitives:FlyoutBase.AttachedFlyout>
                        <MenuFlyout x:Name="DropZoneFlyout">
                            <MenuFlyoutItem Text="Select File..." Click="SelectFile_Click" />
                            <MenuFlyoutItem Text="Select Folder..." Click="SelectFolder_Click" />
                        </MenuFlyout>
                    </primitives:FlyoutBase.AttachedFlyout>
                    <StackPanel VerticalAlignment="Center" Spacing="8">
                        <FontIcon x:Name="UploadFileIcon" Glyph="&#xE7C3;" FontFamily="Segoe Fluent Icons" FontSize="28" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" />
                        <TextBlock x:Name="UploadFileNameText" Text="Click to select file(s) for upload..." TextAlignment="Center" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" HorizontalAlignment="Center" />
                        <TextBlock x:Name="UploadFileSizeText" Text="Drag &amp; drop one or more files here" TextAlignment="Center" FontSize="10" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>

                <!-- Upload Progress -->
                <StackPanel Grid.Row="5" x:Name="UploadProgressPanel" Margin="0,0,0,12" Spacing="6" Visibility="Collapsed">
                    <TextBlock x:Name="UploadStatusText" Text="Uploading..." FontSize="11" Foreground="{StaticResource CyanAccentBrush}" />
                    <ProgressBar x:Name="UploadProgressBar" Height="6" Background="#1D2939" Foreground="{StaticResource CyanAccentBrush}" CornerRadius="3" Minimum="0" Maximum="100" Value="0" />
                </StackPanel>

                <!-- Control Buttons -->
                <Grid Grid.Row="6" ColumnDefinitions="*, *" Margin="0,12,0,0">
                    <Button Click="CloseUploadDialog_Click" Style="{StaticResource OutlineButtonStyle}" Content="Cancel" HorizontalAlignment="Stretch" Margin="0,0,6,0" />
                    <Button Grid.Column="1" x:Name="StartUploadBtn" Click="StartUpload_Click" Style="{StaticResource CyanButtonStyle}" Content="Start Upload" HorizontalAlignment="Stretch" Margin="6,0,0,0" IsEnabled="False" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
